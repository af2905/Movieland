<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.github.af2905.movieland.presentation.feature.updatefailed.SimpleDialogViewModel" />

    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="@dimen/default_simple_dialog_size"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/title"
            style="@style/TextView.Medium.Bold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/default_margin_medium"
            android:paddingTop="@dimen/default_margin_medium"
            android:paddingEnd="@dimen/default_margin_medium"
            tools:text="Title" />

        <TextView
            android:id="@+id/message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/default_margin_medium"
            android:layout_marginTop="@dimen/default_margin"
            android:layout_marginEnd="@dimen/default_margin_medium"
            android:layout_marginBottom="@dimen/default_margin_medium"
            android:textColorLink="?colorPrimary"
            tools:text="Ошибка соединения. Пожалуйста проверте соединение и обновите страницу" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/close"
            style="@style/MaterialButton.TextButton.Dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingEnd="@dimen/default_margin_small"
            android:paddingStart="@dimen/default_margin_small"
            android:paddingTop="@dimen/default_margin_small"
            android:paddingBottom="@dimen/default_margin_small"
            android:layout_marginBottom="@dimen/default_margin_small"
            android:layout_marginEnd="@dimen/default_margin_small"
            android:layout_gravity="end"
            android:text="@android:string/ok"
            android:textAllCaps="true" />

    </LinearLayout>

</layout>